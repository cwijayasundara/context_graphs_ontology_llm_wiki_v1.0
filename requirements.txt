# --- Core runtime ---
pyyaml>=6.0.3
python-dotenv>=1.2.2          # loads .env at import time
requests>=2.33.1
pytest>=9.0.3

# --- Graph backend (optional; without kuzu the JSONL snapshot still works) ---
kuzu>=0.11.3

# --- Document parsers ---
liteparse>=1.2.1              # default PDF parser (wraps the LiteParse Node.js CLI)
python-docx>=1.2.0            # Word .docx parser
python-pptx>=1.0.2            # PowerPoint .pptx parser
trafilatura>=2.0.0            # HTML → clean markdown extraction
beautifulsoup4>=4.14.3        # link extraction for the web crawler
openpyxl>=3.1.5               # Excel .xlsx record ingester

# --- DeepAgents / LangChain stack ---
deepagents>=0.5.6
langchain>=1.2.17
langchain-core>=1.3.2
langchain-openai>=1.2.1       # used for Moonshot/Kimi/OpenRouter/NVIDIA/DeepSeek
                              # via the OpenAI-compatible endpoint
langchain-google-genai>=4.2.2 # optional Gemini provider

# --- Optional ---
# xlrd>=2.0.2                  # only needed if you re-enable legacy .xls reading
# paper-qa-nemotron            # only for DOCUMENT_PARSER_PROVIDER=nemotron
