pyyaml>=6.0
python-dotenv>=1.0   # loads .env at import time
kuzu>=0.4.0          # optional; jsonl snapshot works without it
pytest>=8.0
requests>=2.32
liteparse>=0.1       # Python wrapper around the LiteParse Node.js CLI
python-docx>=1.1     # Word .docx parser
python-pptx>=1.0     # PowerPoint .pptx parser
openpyxl>=3.1        # Excel .xlsx record ingester
trafilatura>=2.0     # HTML → clean markdown extraction
beautifulsoup4>=4.14 # link extraction for the web crawler

# DeepAgents stack
deepagents>=0.5.3
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0   # used for Moonshot/Kimi via OpenAI-compatible endpoint
langchain-google-genai>=2.0.0   # optional Gemini provider

# Optional for DOCUMENT_PARSER_PROVIDER=nemotron:
# paper-qa-nemotron
